// src/lib/context.js
import React, { createContext, useContext } from "react";
var IconlyContext = createContext();
IconlyContext.displayName = "IconlyIconlyContext";
var IconlyProvider = ({
  children,
  primaryColor,
  secondaryColor,
  set,
  size,
  stroke
}) => {
  const defaulValue = {
    primaryColor: primaryColor || "currentColor",
    secondaryColor: secondaryColor || null,
    set: set || "light",
    size: size || "medium",
    stroke: stroke || "regular"
  };
  return /* @__PURE__ */ React.createElement(IconlyContext.Provider, { value: defaulValue }, children);
};
var useIconlyTheme = () => {
  const context = useContext(IconlyContext);
  if (context === void 0) {
    throw new Error(`useIconlyTheme must be used within a IconlyProvider`);
  }
  return context;
};

export {
  IconlyContext,
  IconlyProvider,
  useIconlyTheme
};
